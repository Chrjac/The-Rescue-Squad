using UnityEngine;
using System.Collections;

public class GameGUI : MonoBehaviour {
	/*
	public Transform experienceBar;
	public TextMesh levelText;
	public TextMesh ammoText;

	public void SetPlayerExperience(float percentToLevel, int Player Level){
		levelText.text = "Level" + playerLevel;
		experienceBar.localScale = new Vector3(percentToLevel, 1, 1);
	}

	public void SetAmmoInfo(int totalAmmo, int ammoMag){
		ammoText.text = ammoMag + "/" + totalAmmo;
	}
	*/
}
